- openshift_v1_route:
    name: dynamodb-sample-app
    namespace: '{{ namespace }}'
    state: absent
- k8s_v1_service:
    name: dynamodb-sample-app
    namespace: '{{ namespace }}'
    state: absent
- openshift_v1_deployment_config:
    name: dynamodb-sample-app
    namespace: '{{ namespace }}'
    state: absent
