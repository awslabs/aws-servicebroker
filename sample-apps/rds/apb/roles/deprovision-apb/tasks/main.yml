- openshift_v1_route:
    name: rds-sample-app
    namespace: '{{ namespace }}'
    state: absent
- k8s_v1_service:
    name: rds-sample-app
    namespace: '{{ namespace }}'
    state: absent
- openshift_v1_deployment_config:
    name: rds-sample-app
    namespace: '{{ namespace }}'
    state: absent
